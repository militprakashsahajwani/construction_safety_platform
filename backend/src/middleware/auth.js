import jwt from 'jsonwebtoken';export function auth(q,s,n){const t=q.headers.authorization?.split(' ')[1];if(!t)return s.status(401).json({message:'Authentication required'});try{q.user=jwt.verify(t,process.env.JWT_SECRET);n()}catch{return s.status(401).json({message:'Invalid token'})}}export const allow=(...r)=>(q,s,n)=>r.includes(q.user.role)?n():s.status(403).json({message:'Insufficient permission'});
