import nodemailer from 'nodemailer'; import {prisma} from './prisma.js';
export async function notify(userIds,message,type='info'){const ids=[...new Set(userIds.filter(Boolean))];if(!ids.length)return;await prisma.notification.createMany({data:ids.map(userId=>({userId,message,type}))});if(process.env.SMTP_HOST){const users=await prisma.user.findMany({where:{id:{in:ids}},select:{email:true}});const transporter=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:false,auth:{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}});await transporter.sendMail({from:process.env.MAIL_FROM,to:users.map(x=>x.email).join(','),subject:'SafetyHub alert',text:message}).catch(()=>{});}}
