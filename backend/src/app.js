import express from 'express';import cors from 'cors';import helmet from 'helmet';import rateLimit from 'express-rate-limit';import auth from './routes/auth.js';import sites from './routes/sites.js';import incidents from './routes/incidents.js';import inspections from './routes/inspections.js';import notifications from './routes/notifications.js';import dashboard from './routes/dashboard.js';import admin from './routes/admin.js';import {errorHandler} from './middleware/error.js';
const app=express();app.use(helmet());app.use(cors({origin:process.env.FRONTEND_URL?.split(',')||true}));app.use(express.json({limit:'1mb'}));app.get('/api/health',(q,s)=>s.json({status:'ok'}));app.use('/api/auth',rateLimit({windowMs:15*60*1000,max:100,message:{message:'Too many authentication attempts'}}),auth);app.use('/api/sites',sites);app.use('/api/incidents',incidents);app.use('/api/inspections',inspections);app.use('/api/notifications',notifications);app.use('/api/dashboard',dashboard);app.use('/api/admin',admin);app.use(errorHandler);export default app;
